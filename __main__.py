# from sync_engine.models.base import Base
# import settings
# from sqlalchemy.engine import create_engine
#
# settings = settings.Settings()
# engine = create_engine(settings.DB_DSN)
#
# Base.metadata.create_all(engine)